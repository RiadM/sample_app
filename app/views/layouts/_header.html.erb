<header class="navbar navbar-fixed-top navbar-inverse">
  	<div class="container">
  		<%= link_to "Uruk", root_path, id: "logo" %>
  		<nav>
  			<ul class="nav navbar-nav navbar-right">
  				<li><%= link_to "Accueil", root_path%></li>
  				<li><%= link_to "Contact", new_contact_path%></li>
          
          <% if logged_in? %>
            <li><%=link_to "Utilisateur",users_path %></li>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                Account <b class="caret"></b>
                </a>
                  <ul class="dropdown-menu">
                    <li><%= link_to "Profile", current_user%></li>
                    <li><%= link_to "Paramètres", edit_user_path(current_user)%></li>
                    <li class="divider"></li>
                    <li>
                      <%= link_to "Se déconnecter", logout_path, method: :delete%>
                    </li>
                  </ul>
              </li>
            <% else %>
  				    <li><%= link_to "Se connecter", login_path%></li>
  			    <% end %>
        </ul>
  		</nav>
  	</div>
   </header>